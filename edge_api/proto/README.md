# Proto Location

Canonical protobuf sources now live under `/contracts/proto`.

For ingest contracts use:

- `/contracts/proto/ingest.proto`

For agent and enrollment contracts use:

- `/contracts/proto/agent.proto`

TODO: update Go code generation in `edge_api` to consume shared proto files directly instead of the historical in-tree path.
