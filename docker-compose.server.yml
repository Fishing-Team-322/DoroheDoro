services:
  defay1x9-api:
    build:
      context: ./defay1x9
      dockerfile: Dockerfile
    env_file:
      - ./defay1x9/.env.server
    ports:
      - "127.0.0.1:18080:8080"
    restart: unless-stopped
